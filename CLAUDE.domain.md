## Dominio de este repo

- **Qué es**: agent-pipeline — el repo central que gobierna el pipeline de agentes de sus consumidores (finplan, wmcb): workflows reusables, mandatos vendorizados (servidos por graft en runtime, AP-009), hooks, plantillas y contratos. Registro normativo: `docs/decisions.md` (AP-001..). **Este repo es consumidor de sí mismo** (AP-012): sus issues `process-proposal` los ejecuta su propio Creator.
- **Rama base**: `main` (protegida: PR + CI verde + merge HUMANO — sin excepción; aquí no hay automerge).
- **Comandos de verificación local** (los 5 que corre `ci.yml`, siempre, antes del push): `npm ci && node scripts/check-yaml.mjs && node scripts/check-contracts.mjs && node scripts/check-labels.mjs && node scripts/check-embedded-js.mjs && node scripts/check-hooks.mjs`. (Eran 4 hasta que `check-hooks.mjs` entró en `ci.yml`; la lista local se quedó corta y los bodies de PR heredaron el «los 4 checks» — corregido en la review de #165.)
- **Zonas de rigor especial**: `vendored/` — TODO diff aquí despliega a los DOS consumidores en su siguiente run, sin gradualidad; edítalo como si empujaras a producción de dos sistemas a la vez. `templates/workflow-contracts.json` — si tu PR cambia la superficie `workflow_call` de un reusable, el manifiesto se actualiza en el MISMO PR (AP-003; el CI lo bloquea).
- **Workflows y actions** (`.github/workflows/**`, `.github/actions/**`): desde 2026-07-15 (enmienda AP-012) tu push a RAMA los admite. *(2026-07-28, AP-064 — MEDIDO EN CONTRA: GitHub rechazó el push con `refusing to allow a GitHub App to create or update workflow .github/workflows/watchdog.yml without workflows permission`. La enmienda no cubre ese path: la App sigue sin el permiso `workflows`. Mientras esto siga aquí, planifica un cambio de workflow como PENDIENTE HUMANO — viaja como `docs/patches/*.patch` y `check-patches` lo vigila. Pendiente de reconciliar por el propietario: o se concede el permiso, o se retira la frase anterior.)* El merge es humano SIEMPRE y te es materialmente imposible. Rigor extra: blast radius detallado en el body del PR y **los checks de `ci.yml`** en local antes del push — la lista viva es la de «Comandos de verificación local» de arriba, nunca un número repetido aquí (el «4» que vivía en esta frase era un heredado ya corregido en la review de #165 y sobrevivió a la corrección por estar duplicado; desde AP-064 `check-embedded-js` invoca además `check-patches` y `check-resolve-detection`, luego el número vuelve a ser el dato equivocado que copiar) — un workflow roto aquí tumba el pipeline de dos repos (histórico: SyntaxError con CI verde, timeout que mató la capa 2).
- **Convenciones**: mensajes de commit en español técnico como el corpus; toda decisión de doctrina nueva o enmendada se registra en `docs/decisions.md` con número AP.
